# jwt-decryptor
A utility that decrypts encrypted JWEs using RSA OAEP (256, 384, 512) and ECDH algorithms. It loads the required private key, unwraps the JWE, and outputs the decrypted payload in JSON form for validating the JWT
